## 2026-03-25 - [Add Timeout and Error Sanitization to External API Call]
**Vulnerability:** The `fetch` call to `https://api.argentinadatos.com` in `App.tsx` had no timeout configuration, meaning the application could hang or suffer resource exhaustion if the external API responds very slowly or indefinitely. Additionally, raw error objects were being passed to `console.error()`, which can leak internal stack traces or details to the browser console. Finally, the project had an outdated dependency `brace-expansion` causing an audit warning.
**Learning:** External API integrations without timeout limits and unsanitized error logging are common medium-severity security and stability risks in frontend components. Fixing these provides better defense in depth.
**Prevention:** Always implement an `AbortController` and `setTimeout` alongside `fetch` calls, and avoid logging full error object details directly to the console in production environments.

## 2026-04-04 - [Add NGINX Security Headers in Production Dockerfile]
**Vulnerability:** The production `Dockerfile` configured an NGINX server without important HTTP security headers (like `X-Frame-Options`, `X-Content-Type-Options`, `Content-Security-Policy`, and `Referrer-Policy`) and leaked the NGINX version via the `Server` header. This could leave the application open to Clickjacking, MIME-type sniffing, XSS, and information disclosure attacks.
**Learning:** Containerized static websites often use basic NGINX configurations that default to insecure settings. Even purely static sites should enforce a strong Content Security Policy and basic anti-framing headers to follow defense-in-depth principles.
**Prevention:** Always explicitly configure secure headers (e.g., CSP, X-Frame-Options, X-Content-Type-Options) and disable `server_tokens` in NGINX container configurations.

## 2026-04-05 - [Strict Trust Boundary Validation for Local Storage and API Input]
**Vulnerability:** External APIs and client-side storage (`localStorage`) act as trust boundaries but were being consumed implicitly without validation. In `useTheme.ts`, untrusted localStorage data was cast directly and applied to `classList.add()`, which could trigger `DOMException` if injected with space-separated malicious strings. In `App.tsx`, the raw JSON array from the external API was mapped to the DOM without structure or length validation, posing a minor client-side DoS risk or unexpected behavior if the API output changed.
**Learning:** Implicit trust in external data sources (even APIs considered "official") or local storage allows data integrity issues to become execution or runtime exceptions. Validation at the trust boundary is a core pillar of defense-in-depth.
**Prevention:** Always validate format, length, and types when crossing a trust boundary, ensuring data conforms exactly to expected internal structures before it interacts with standard library functions like `classList.add()` or React state rendering.

## 2026-04-06 - [Add Strict Validation for External API Payload]
**Vulnerability:** The raw JSON array fetched from the external API (`https://api.argentinadatos.com`) was being implicitly trusted. While structural typing was present, string lengths were not bound and the `fecha` date string wasn't checked for parseability. An invalid date payload could lead to a `NaN` evaluation or exception within the `Date.toLocaleDateString()` component method, causing an unhandled React crash (client-side DoS).
**Learning:** Implicit trust in external API formats (even "official" ones) poses a stability risk, particularly when mapping data directly to Date object methods that throw errors on bad inputs. Length validation is also a crucial defense-in-depth tactic against payload tampering that causes rendering lag or memory exhaustion.
**Prevention:** Always validate external payloads rigorously. Check format, size, and ensure dates parse without producing `NaN` before rendering them into the UI state.

## 2026-04-07 - [Harden CSP and Prevent Client-Side Race Condition]
**Vulnerability:** The NGINX Content-Security-Policy header in the `Dockerfile` included `'unsafe-inline'` in the `script-src` directive, weakening defense against XSS. In `App.tsx`, the `useEffect` hook handling the external API fetch lacked an abort cleanup function, which could cause a client-side race condition and resource exhaustion if the user rapidly toggled between years.
**Learning:** Even modern React apps (which don't need inline scripts in production) can inherit insecure default settings if not explicitly stripped out of custom NGINX configurations. Also, external fetches within `useEffect` must always be cleanly aborted on dependency changes to prevent stale states from overwriting newer ones and exhausting client/server resources.
**Prevention:** Always remove `'unsafe-inline'` from CSP `script-src` unless absolutely required and explicitly document why. Always return a cleanup function utilizing `AbortController.abort()` for any async requests executed inside `useEffect`.

## 2026-04-08 - [Add HSTS and Permissions-Policy Security Headers]
**Vulnerability:** The production NGINX configuration in `Dockerfile` was missing the `Strict-Transport-Security` (HSTS) and `Permissions-Policy` headers. Missing HSTS can expose users to Man-in-the-Middle (MitM) attacks during initial connection, while missing Permissions-Policy allows unauthorized or potentially malicious use of browser features like geolocation, camera, and microphone.
**Learning:** Proper security headers form a crucial layer of defense for web applications. HSTS ensures that browsers only connect over HTTPS, preventing downgrade attacks. Permissions-Policy actively restricts the features available to the site, reducing the attack surface.
**Prevention:** Always enforce HSTS and define a strict Permissions-Policy in web server configurations to restrict unauthorized browser feature usage and ensure encrypted communication.

## 2026-04-09 - [Harden CSP object-src and Prevent Dynamic Fetch SSRF]
**Vulnerability:** The Content-Security-Policy lacked `object-src 'none'` and `base-uri 'none'`, allowing potential plugin exploitation or base URL hijacking. Additionally, a dynamic parameter (`year`) was interpolated directly into an external `fetch` request without range/type boundary validation. Although managed by React state, lack of validation makes this a target for Server-Side Request Forgery (SSRF) or malformed payload crashes if the state machine logic is ever bypassed or directly fed user input via deep linking.
**Learning:** Defense-in-depth requires explicit parameter validation immediately prior to network execution, regardless of UI-layer constraints. Content-Security-Policy must explicitly ban legacy features (like plugins via `object-src`) even in modern stacks.
**Prevention:** Always strictly validate dynamic variables (e.g., bounds checking numbers) immediately before network interpolation. Explicitly set `object-src 'none'`, `base-uri 'none'`, and `upgrade-insecure-requests` in all CSP configurations.

## 2026-04-10 - [Strict Length Bounds Before Date Parsing]
**Vulnerability:** The date string `h.fecha` received from the external API was being appended with a time string and directly passed to the native `Date` constructor and `.getTime()` method in `App.tsx` without length limits. A maliciously crafted, extremely long string (e.g., millions of characters) masquerading as a date from a compromised API response could trigger excessive memory allocation and a potential ReDoS-like CPU spike during date parsing, leading to an unhandled exception or browser crash (Client-side DoS).
**Learning:** Native parsing functions (like `new Date()`, `JSON.parse()`, etc.) are vulnerable to resource exhaustion if fed unboundedly large inputs. Relying solely on `typeof === 'string'` is insufficient validation at a trust boundary.
**Prevention:** Always enforce strict length bounds (e.g., `h.fecha.length === 10` for `YYYY-MM-DD` formats) on strings immediately before they are consumed by native parsers or constructors.

## 2026-04-11 - [Fail Securely on Blocked LocalStorage]
**Vulnerability:** The `useTheme` hook directly accessed `localStorage` to read and persist the user's theme preference. If a user configured their browser to block third-party cookies or site data (e.g., strict privacy mode), accessing `localStorage` synchronously throws a `SecurityError`. Since these calls were outside a `try...catch` block, this unhandled exception would crash the entire React application on load (Client-side DoS).
**Learning:** Browser APIs that interact with storage or sensitive permissions can throw synchronous exceptions based on user privacy settings. A core security principle is to "fail securely"—the application should gracefully degrade to default behavior (e.g., using the default system theme) rather than failing catastrophically and denying service.
**Prevention:** Always wrap `localStorage` and `sessionStorage` operations (`getItem`, `setItem`, `removeItem`) in `try...catch` blocks. Ensure the fallback behavior maintains usability.
## 2026-04-14 - [Validate External API Content-Type]
**Vulnerability:** The `fetch` call to `https://api.argentinadatos.com` in `App.tsx` did not specify an `Accept` header or validate the `Content-Type` of the response before parsing it as JSON. This could lead to MIME confusion if the API returns an unexpected format (e.g., HTML error page), potentially causing unpredictable parsing errors or exposing the application to injection attacks if the response is rendered directly.
**Learning:** Implicitly trusting the content type of external APIs is a defense-in-depth risk. Even if the payload format is validated later, attempting to parse an unexpected format (like HTML as JSON) can cause unhandled exceptions or crash the application.
**Prevention:** Always explicitly request expected formats (e.g., `Accept: application/json`) and strictly validate the `Content-Type` of responses (e.g., `res.headers.get('content-type')?.includes('application/json')`) before processing them.

## 2026-04-15 - [Fix NaN Bypass in Numeric Validation]
**Vulnerability:** The numeric validation for the `year` parameter in `src/App.tsx` used `typeof year !== 'number'`. This check is vulnerable to bypasses because in JavaScript, `NaN` and `Infinity` are technically of type `'number'`. If `year` was `NaN`, both `year < 2000` and `year > 2100` would evaluate to `false`, allowing the parameter to bypass the boundary checks completely.
**Learning:** Using `typeof x === 'number'` is insufficient for validating numbers when boundaries are involved, as `NaN` fails numerical comparisons (returning `false`) but passes the type check, which can lead to unexpected behavior or bypasses in validation logic.
**Prevention:** Always use `Number.isSafeInteger()` or `Number.isFinite()` when validating numeric input, especially when ensuring the value falls within specific boundaries, to prevent `NaN` and `Infinity` from bypassing checks.

## 2026-04-19 - [Harden CSP and Add Cross-Origin Isolation Headers]
**Vulnerability:** The `Content-Security-Policy` header in `Dockerfile` lacked `form-action 'none'` and `frame-ancestors 'none'`, exposing the app to potential form injection and modern clickjacking attacks. Additionally, the application lacked Cross-Origin isolation headers (`Cross-Origin-Opener-Policy` and `Cross-Origin-Resource-Policy`), making it potentially vulnerable to side-channel attacks like Spectre if cross-origin resources were loaded improperly.
**Learning:** Even static Single Page Applications (SPAs) should implement comprehensive CSP directives to disable unused features (like forms or framing) as a defense-in-depth measure. Implementing Cross-Origin isolation headers is an essential modern security practice to mitigate advanced side-channel attacks, ensuring that resources are only loaded from the same origin unless explicitly permitted.
**Prevention:** Always include restrictive directives like `form-action 'none'` and `frame-ancestors 'none'` in CSP for applications that do not use forms or framing. Implement `Cross-Origin-Opener-Policy` and `Cross-Origin-Resource-Policy` headers set to `same-origin` to enforce strict cross-origin isolation.

## 2026-04-20 - [Harden CSP style-src and Prevent CSS Injection]
**Vulnerability:** The NGINX Content-Security-Policy header in the `Dockerfile` included `'unsafe-inline'` in the `style-src` directive, weakening defense against CSS Injection. CSS Injection can be used to alter the visual appearance of a page, steal sensitive data, or perform clickjacking.
**Learning:** Even if inline styles are not used intentionally by developers, allowing them through CSP leaves the application vulnerable to malicious CSS injection if user input is reflected in the DOM without proper sanitization.
**Prevention:** Always remove `'unsafe-inline'` from CSP `style-src` unless absolutely required and explicitly document why. If inline styles are needed, prefer using CSS classes instead, or consider using nonces or hashes if inline styles are strictly required.

## 2026-04-21 - [Strict Payload Validation Against XSS at Network Boundary]
**Vulnerability:** External API payloads in `holidayService.ts` were only validated for type and length. Without an explicit schema check for date formatting (`/^\d{4}-\d{2}-\d{2}$/`) and explicit rejection of HTML characters (`<` and `>`) in text fields (`tipo`, `nombre`), the application was theoretically vulnerable to Cross-Site Scripting (XSS) if the external API was compromised or if its data was reflected in the DOM without sanitization, and parsing errors from malformed date strings.
**Learning:** Checking length and `typeof === 'string'` is insufficient to prevent injection attacks when consuming third-party data. Defense-in-depth requires strictly validating the exact format of structured data (like dates) and explicitly sanitizing or rejecting potentially malicious characters (like HTML tags) immediately at the network boundary, before the data enters application state.
**Prevention:** Always apply strict Regex schema checks to structured strings and block/sanitize HTML characters (`<`, `>`) for any text fields derived from external inputs before they are allowed into the application's React state.

## 2026-04-22 - [Validate Content-Length Before JSON Parsing]
**Vulnerability:** The application blindly called `res.json()` on the external API response. If the external API were compromised and returned an unbounded massive payload, `res.json()` would attempt to load and parse the entire payload into client memory. This could crash the browser tab due to memory exhaustion (Client-Side DoS).
**Learning:** Relying purely on DOM/Array limits after data parsing (e.g. `data.length > MAX_HOLIDAYS`) is insufficient defense. Memory exhaustion vulnerabilities occur during the deserialization (`res.json()`) step itself.
**Prevention:** Always validate the `Content-Length` header (if present) before invoking memory-intensive parser functions like `res.json()` or `res.text()` when fetching external data.

## 2026-04-29 - [Fix DoS Protection Bypass in Content-Length Validation]
**Vulnerability:** The `Content-Length` validation logic checked `!Number.isNaN(contentLength) && contentLength > 50000`. If an attacker provided a malformed `Content-Length` header that parsed to `NaN`, the check evaluated to `false`, bypassing the 50KB limit. This could allow a massive payload to be passed to `res.json()`, causing memory exhaustion and a Client-Side DoS.
**Learning:** When enforcing security boundaries based on parsed values, you must fail securely on invalid parser outputs. Using `!NaN && condition` causes the entire check to fail implicitly, opening a bypass.
**Prevention:** Always check for invalid values (like `NaN`) explicitly and fail securely (e.g., `Number.isNaN(contentLength) || contentLength > 50000`).

## 2026-05-01 - [Enforce CORS on External Scripts]
**Vulnerability:** The external tracking script (`https://umami.johatech.ar/script.js`) was loaded without the `crossorigin="anonymous"` attribute in `index.html`. Without this attribute, the browser sends the user's cookies (if any exist for that third-party domain) with the request, potentially leaking session state or PII to the third party. Furthermore, without CORS enforcement, any errors occurring within the external script are muted by the browser (reporting as "Script error."), hindering observability of potential malicious modifications to the third-party script.
**Learning:** Loading third-party scripts without explicit CORS configuration is a privacy and observability risk. It defaults to sending credentials (cookies) to the third party and obfuscates error details, which are critical for detecting if a third-party dependency has been compromised or is behaving unexpectedly.
**Prevention:** Always include the `crossorigin="anonymous"` attribute on `<script>` tags that load resources from external domains to ensure credentials are not sent and to enable detailed error reporting for security monitoring.

## 2026-05-05 - [Fix Client-Side DoS via Stream Size Limits]
**Vulnerability:** The application attempted to prevent memory exhaustion by checking the `Content-Length` header before parsing API responses. However, if an attacker compromised the external API, they could omit the `Content-Length` header entirely (e.g., using chunked transfer encoding) or spoof it to a small value, bypassing the check and causing `res.json()` to buffer an unbounded payload into client memory, leading to a browser crash.
**Learning:** Validating headers like `Content-Length` is insufficient to prevent memory exhaustion because they are untrusted and optional. The only robust way to limit payload size is to track the actual bytes as they are received.
**Prevention:** Instead of relying on `res.json()` and headers, use the Fetch API's `ReadableStream` (`res.body.getReader()`) to read chunks, accumulate the total length, and explicitly `cancel()` the stream and throw an error if a hard byte limit is exceeded during transmission.
## 2026-05-02 - [Enforce CORS and Prevent Credential Leakage on External Scripts]
**Vulnerability:** The application was loading an external script (`https://umami.johatech.ar/script.js`) without the `crossorigin` attribute. This allows the browser to potentially send implicit credentials (like cookies or HTTP authentication) to the third-party domain and prevents detailed error reporting to the application if the script fails, making it harder to detect script-based issues or tampering.
**Learning:** Loading third-party scripts without `crossorigin="anonymous"` can unintentionally leak user session information to external servers depending on browser behavior and same-site cookie settings. It also restricts the `window.onerror` handler from seeing detailed stack traces for cross-origin script errors.
**Prevention:** Always include the `crossorigin="anonymous"` attribute on external `<script>` tags to enforce Cross-Origin Resource Sharing (CORS) policies, prevent credential leakage, and enable detailed error reporting.

## 2026-05-06 - [Strict Property Mapping on External API Payloads]
**Vulnerability:** The application validated the external API payload ensuring expected fields existed and matched types, but the resulting object was returned as-is and implicitly typed as `Holiday[]`. If the external API returned an object containing the required properties but also injected additional undocumented or malicious properties (e.g., prototype pollution payloads like `__proto__`), those properties would be carried directly into the application's React state and memory.
**Learning:** Checking for the existence of required fields does not guarantee the absence of malicious fields. Implicit typecasting leaves applications vulnerable to processing and persisting unexpected properties from third-party APIs.
**Prevention:** Always explicitly map over validated third-party payloads to construct brand-new objects, strictly copying over ONLY the exact properties expected by the application's domain logic, thereby dropping any extraneous data.

## 2026-05-09 - [Securely Parse UTF-8 Stream with TextDecoder]
**Vulnerability:** The application was instantiating `TextDecoder` without the `{ fatal: true }` option. This causes the decoder to silently replace invalid UTF-8 byte sequences with the Unicode replacement character (U+FFFD). In a security context, silently mutating malformed network data can lead to subtle parsing vulnerabilities, data corruption, or bypasses if the application logic expects exact string matching on potentially manipulated payloads.
**Learning:** Silently handling data parsing errors at the network boundary violates the "fail securely" principle. If an external payload contains malformed bytes, it is safer to reject the entire payload than to attempt to guess or substitute data.
**Prevention:** When using `TextDecoder` to parse untrusted external API data, always instantiate it with `{ fatal: true }` (e.g., `new TextDecoder('utf-8', { fatal: true })`) to strictly enforce valid character encoding and securely throw an error on malformed input.

## 2026-05-10 - [Enforce Subresource Integrity on External Scripts]
**Vulnerability:** The external tracking script (`https://umami.johatech.ar/script.js`) was loaded without Subresource Integrity (SRI) validation. If the third-party analytics server were compromised and the script modified to include malicious code, the application would blindly execute it, leading to a supply-chain Cross-Site Scripting (XSS) vulnerability.
**Learning:** Loading third-party scripts without integrity checks creates a massive blind spot in application security. Relying solely on `crossorigin="anonymous"` protects credentials but does not guarantee the script's contents remain benign.
**Prevention:** Always include the `integrity` attribute with a cryptographic hash (e.g., `sha384-...`) on external `<script>` tags to ensure the browser strictly verifies the fetched resource matches the expected hash before execution.

## 2026-05-18 - [Run NGINX as Non-Root Unprivileged User]
**Vulnerability:** Running NGINX as the default root user in a Docker container violates the principle of least privilege. If an attacker exploits a vulnerability in NGINX or a web application component, they gain root access within the container, making it easier to attempt container breakouts or interact with the host system.
**Learning:** Default base images often run services as root. Proactively switching to an unprivileged variant significantly limits the blast radius of any potential compromise by ensuring the process only has access to explicitly required resources.
**Prevention:** Instead of using `nginx:stable`, use `nginxinc/nginx-unprivileged:stable-alpine` which runs the NGINX process as a non-root user (UID 101). Update port mappings to bind to non-privileged ports (e.g., 8080 instead of 80) inside the container.

## 2026-06-01 - [Prevent Information Leakage from Default Parsers]
**Vulnerability:** The application was allowing raw parsing functions (`TextDecoder.decode`, `JSON.parse`) to throw default exceptions when processing external network data. If an external payload was malformed, the default error message could leak sensitive internal context, stack traces, or snippets of the malformed payload itself into error tracking systems or application logs.
**Learning:** Default parser errors are meant for developers and often contain verbose details about why a string failed to parse. Exposing these unfiltered errors when handling untrusted data violates the "Fail Securely" principle by potentially leaking information to attackers or polluting logs with attacker-controlled strings.
**Prevention:** Always wrap data deserialization methods (`JSON.parse`, `TextDecoder.decode`) in a `try...catch` block when processing external input, and throw generic, secure error messages (e.g., "Failed to securely parse API response") to avoid leaking internal details.

## 2026-06-03 - [Prevent Prototype Pollution via JSON Reviver]
**Vulnerability:** Even if the application later maps properties to a new object, calling raw `JSON.parse(data)` temporarily instantiates the untrusted data structure into memory as parsed. If an attacker injects `__proto__`, `constructor`, or `prototype` keys into the JSON payload, it can trigger prototype pollution vulnerabilities in downstream libraries or parsing engines before the application has a chance to sanitize it.
**Learning:** Relying solely on explicit property mapping later in the code leaves a brief window where the fully parsed malicious payload exists in memory.
**Prevention:** Always use a reviver function with `JSON.parse` to explicitly drop dangerous prototype-polluting keys (`__proto__`, `constructor`, `prototype`) at the very moment of deserialization. This provides robust defense-in-depth against insecure deserialization.

## 2026-06-05 - [Defense-in-Depth Payload Validation]
**Vulnerability:** External API payloads were validated using basic length checks and object structure. However, there were no strict checks on the formats or content of strings. An attacker compromising the API could inject malformed dates, or embed malicious HTML characters (`<`, `>`) into text fields like `nombre` or `tipo`, which might cause issues during rendering or parsing if React's default escaping is bypassed or if the data is processed down the line.
**Learning:** Basic validation (like length and existence) is not enough for untrusted external payloads. Validating that fields strictly match expected formats ensures that the application doesn't ingest unexpected or potentially malicious content.
**Prevention:** As a defense-in-depth measure, strictly validate external API payloads using exact format schemas (e.g., `^\d{4}-\d{2}-\d{2}$` for dates) and reject payloads containing HTML characters (`<`, `>`) in text fields to prevent Cross-Site Scripting (XSS) or data corruption.
## 2026-06-29 - [Prevent URL Leakage via Fetch Referrer Policy]
**Vulnerability:** The application was using the Fetch API to request data from an external API without specifying a `referrerPolicy`. By default, browsers may include the `Referer` header in outgoing requests, which can leak the full URL of the referring page (including potentially sensitive query parameters or internal path structures) to the third-party server.
**Learning:** Relying on default browser behavior for cross-origin network requests can unintentionally expose internal application context or sensitive data embedded in URLs to external parties.
**Prevention:** When using `fetch` for external API requests, explicitly configure `referrerPolicy: 'no-referrer'` to ensure the browser strictly drops the `Referer` header, protecting application privacy and minimizing information leakage.

## 2026-07-13 - [Strict Numeric Validation Boundaries]
**Vulnerability:** When enforcing numeric validation boundaries (e.g., Content-Length checks or Date timestamp checks), relying on inverted positive conditions like `!isNaN(val) && val < limit` or using the global `isNaN` implicitly evaluates to false on malformed input (like NaN), causing fail-open vulnerabilities and passing invalid data forward.
**Learning:** Global `isNaN` coerces values, which can lead to unexpected true/false returns. Implicit typecasting and fail-open validation logic leave the application vulnerable to processing malformed payload boundaries.
**Prevention:** When enforcing numeric validation boundaries, explicitly handle and fail securely on invalid parser outputs. Prefer `Number.isNaN(val)` over global `isNaN` for strict type checking, and actively prevent `NaN` bypasses (e.g., `Number.isNaN(val) || val > limit`).

## 2026-08-10 - [Prevent Client-Side Cache Poisoning via Data Immutability]
**Vulnerability:** The application used a centralized module-level cache (`holidayCache`) to store and return the parsed holiday data objects. Because the returned array and its objects were mutable, if any downstream React component or third-party script accidentally or maliciously modified the data (e.g., mutating a property directly), it would permanently poison the shared cache in memory for all subsequent renders or components requesting that data.
**Learning:** Returning mutable objects from a shared, centralized cache allows unintentional side-effects and client-side data tampering to propagate across the entire application state. This is especially dangerous when third-party scripts (like analytics trackers) run in the same DOM environment.
**Prevention:** When a service returns data from a shared module-level cache, always enforce deep immutability at the boundary by using `Object.freeze()` on both the individual mapped objects and the returned array itself. This ensures downstream consumers can read but never mutate the centralized state.

## 2026-08-31 - [Prevent Browser Cache Poisoning of External APIs]
**Vulnerability:** The application fetches dynamic data from a third-party external API without explicitly opting out of browser caching. If the external server is temporarily compromised or sends a maliciously crafted payload, the browser might cache the bad response (if the server provides cacheable headers). The application would then continue to use this poisoned cache even after the third-party server is fixed, leading to prolonged exposure or denial of service on the client side.
**Learning:** Depending on third-party servers for correct cache control headers is risky, especially for dynamically loaded configurations or security-sensitive data. The application should actively control its trust boundary regarding cached data.
**Prevention:** To prevent client-side cache poisoning from potentially compromised external APIs, explicitly set `cache: 'no-store'` in `fetch` configurations for dynamic data retrieval, forcing the browser to always fetch a fresh, current copy from the network.
