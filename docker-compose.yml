version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/${GITHUB_REPOSITORY:-tu-usuario/feriadosar}:latest
    container_name: feriadosar-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
