
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/johannmx/feriadosar:latest
    container_name: feriadosar-frontend
    ports:
      - "8080:8080"
    restart: unless-stopped
